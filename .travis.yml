language: python
cache: pip
python:
- 3.6
install: pip install -r requirements.txt
script:
  - cd src/
  - echo "{}" > secrets.json
  # run unit tests only
  - pytest
after_success:
  - codecov
notifications:
  email: false